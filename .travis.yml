sudo: required
services:
  - docker

before_install:
  - docker build -t adrianorft/adriano-react -f Dockerfile.dev .

script:
  - docker run -e CI=true USERNAME/docker-react npm run test
  # script:
  # - docker run USERNAME/docker-react npm run test -- --coverage

  # script:
  # - docker run -e CI=true USERNAME/docker-react npm run test

  # language: generic 